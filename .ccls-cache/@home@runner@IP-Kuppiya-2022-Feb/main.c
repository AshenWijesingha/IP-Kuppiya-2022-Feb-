# include <stdio.h>

int main(void){

  int counter = 1;
  int choice = 1;
  int val = 0;

    //sentinal control repetition
    while(choice == -1){ // check for the value
      //statements 
    }

    //counter control repetition
    if(counter <= 10){
      if(choice == -1){
        val = 1; 
      }
    }
/*  while( choice != -1 )
      {
        if( counter > 10 )

          printf("%d \n", counter);
          ++counter;
          choice = -1;
        
      }
  
    // while (val == 1){ //check for the count  

    // printf("%d \n", counter);
    // ++counter;
    // choice = 1;

    // if(counter <= 10){
    //   if(choice == -1){
    //     val = 1; 
    //   }
    // }  
      
    // }

    // printf("End of while loop");
  
    // do{
      
    //     printf("%d \n", counter);
    //     ++counter;
      
    //   } while (counter <= 10);

    // printf("End of Do-while loop");
  
  return 0;
} 
